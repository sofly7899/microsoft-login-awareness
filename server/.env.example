# Ejemplo de configuración para el servidor de captura
# COPIA ESTE ARCHIVO A .env y rellena los valores antes de ejecutar

# Puerto donde correrá el servidor (ej: 3000)
PORT=3000

# Origen permitido CORS (ajusta al dominio interno donde alojarás el frontend)
CORS_ALLOW_ORIGIN=http://intranet.yourcompany.local

# SMTP (usa el servidor SMTP interno de tu empresa)
SMTP_HOST=smtp.yourcompany.local
SMTP_PORT=25
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=

# Correo destino donde recibirás las capturas
TO_EMAIL=sofly7899@gmail.com

# Email desde el que se enviarán (puede ser un buzón interno)
MAIL_FROM=phishing-awareness@yourcompany.local
